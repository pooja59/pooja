#include <stdio.h>
int main()
{
    int num;
    printf("Enter a number: ");
    scanf("%d", &num);

    if (number <= 0)
    {
        if (number == 0)
            printf("it is  0");
        else
            printf(" it is negative number.");
    }
    else
        printf("it is positive number.");
    return 0;
}

